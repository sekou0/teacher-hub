/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import SearchComponent from './SearchComponent';

storiesOf('SearchComponent', module).add('default', () => <SearchComponent />);
